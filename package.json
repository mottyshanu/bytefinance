{
  "name": "bytefinance",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build && cd ../server && npx prisma generate && cp -r ../client/dist/* public/",
    "start": "cd server && node index.js"
  },
  "engines": {
    "node": "20.x"
  }
}